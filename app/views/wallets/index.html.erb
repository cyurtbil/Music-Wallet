<div id="wlist">
	<% @wallets.each do |wallet| %>
		<h2><%= link_to wallet.name, wallet_path(wallet), class: 'wallets' %></h2>
	<% end %>

	<%= link_to 'Add Wallet', new_wallet_path, class: 'actions' %>
</div>

